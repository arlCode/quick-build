{
  "name": "quick-build",
  "version": "1.0.0",
  "description": "Environment constructed for inline styling.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gulp": "^4.0.0",
    "gulp-sass": "^4.0.2",
    "gulp-load-plugins": "^1.5.0"
  },
  "dependencies": {
    "dotenv": "^7.0.0"
  }
}
